<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Bocaito.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Bocaito"
    Shell.FlyoutBehavior="Flyout"
    Title="Bocaito">

    <ShellContent Title="LogIn" ContentTemplate="{DataTemplate local:LogInPage}" Route="login" />
    <ShellContent Title="SignUp" ContentTemplate="{DataTemplate local:SignUpPage}" Route="signup" />
    <ShellContent Title="ForgotPassword" ContentTemplate="{DataTemplate local:ForgotPasswordPage}" Route="forgotpassword" />
    <ShellContent Title="ForgotPassword" ContentTemplate="{DataTemplate local:ForgotPassword2Page}" Route="forgotpassword2" />
    <!--<TabBar>
        <ShellContent Title="Menu" ContentTemplate="{DataTemplate local:MenuPage}" />
        <ShellContent Title="Pedidos" ContentTemplate="{DataTemplate local:PedidosPage}" />
        <ShellContent Title="Reservas" ContentTemplate="{DataTemplate local:ReservasPage}" />
        <ShellContent Title="Cuenta" ContentTemplate="{DataTemplate local:CuentaPage}" />
    </TabBar> -->
</Shell>
